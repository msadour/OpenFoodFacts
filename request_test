Verifier que l'aliment de subsitue correspond bien à la categorie de l'aliment
*****************************************
SELECT Product.name, Category.name
FROM Product, Product_Category, Category
WHERE Product.id = Product_Category.id_product
AND Category.id = Product_Category.id_category
AND Category.name = ""
AND Product.name IN ('', '');


Verifier les aliment present dans une categorie par id de categorie
*****************************************
SELECT Product.name, Product.nutri_score, Category.name
FROM Product, Product_Category, Category
WHERE Product.id = Product_Category.id_product
AND Category.id = Product_Category.id_category
AND Category.id = ;


Verifier les aliment present dans une categorie par nom de categorie
*****************************************
SELECT Product.name, Product.nutri_score, Category.name
FROM Product, Product_Category, Category
WHERE Product.id = Product_Category.id_product
AND Category.id = Product_Category.id_category
AND Category.name = '';


Voir les aliments avec leurs categorie + nutri_score
*****************************************
SELECT Category.name AS 'nom categorie',Product.name, Product.nutri_score
FROM Product, Product_Category, Category
WHERE Product.id = Product_Category.id_product
AND Category.id = Product_Category.id_category
ORDER BY Category.name;


Vider la base de donnée (DROP DATABASE  prennait trop de temps)
*****************************************
DELETE FROM Food_User;
DELETE FROM Food_Category;
DELETE FROM User;
DELETE FROM Category;
DELETE FROM Food;
